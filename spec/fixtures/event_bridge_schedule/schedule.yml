shared: &shared
  daily_backup_to_s3:
    command: 'jobmon --estimate-time=3000 sg_tiny_backup:backup'
    cron: 'cron(30 1 * * ? *)'
    container_type: medium

staging:
  <<: *shared

production:
  <<: *shared